#include <stdio.h>
#include <stdlib.h>
#include "tool.h"

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
 figure *acharfigure = {0};
 float x = ploscha (acharfigure);
	printf("Ploscha kola = %f", x);
}

